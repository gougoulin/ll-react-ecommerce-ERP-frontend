import {
  forwardRef,
  ReactComponentElement,
  RefAttributes,
  RefObject,
  FC
} from "react";

// export const withRef = (Comp: FC, ref: RefAttributes<unknown>) => {
//   let out = forwardRef((props, ref) => {
//     return <Comp {...props} ref={ref} />;
//   });
//   return out;
// };
