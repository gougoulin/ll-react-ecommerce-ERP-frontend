import styled from "styled-components";
import { colors, space } from "../../assets/css/params";
const LoginTitle = styled.h2`
  text-align: center;
  font-size: 4rem;
  margin: ${space.xlSpace} 0 ${space.lSpace};
  font-weight: 700;
  color: ${colors.gray11};
`;
export default LoginTitle;
